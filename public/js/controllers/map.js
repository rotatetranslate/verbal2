angular.module('MyApp')
  .controller('MapCtrl', MapCtrl);

MapCtrl.$inject = ['$http', '$scope', '$rootScope', '$location', '$window', '$auth', 'Account'];

function MapCtrl($http, $scope, $rootScope, $location, $window, $auth, Account) {
  google.maps.Geocoder.geocode({
    locationL
  })
};
